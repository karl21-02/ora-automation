# Caddyfile for Mimir API
# Automatic HTTPS via Let's Encrypt

{
    email manuna530@gmail.com
}

api.mimisbrunn.com {
    # API runs on host network for GCP metadata access
    # Use Docker's host gateway (172.17.0.1) or localhost via host network
    reverse_proxy 172.17.0.1:8000
}
