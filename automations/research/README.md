# research

R&D 주제 선정 자동화 모듈.

현재 실행 엔진(분석/토론/보고서/검증)은 `ora-automation/` 루트의 `Makefile`, `src`, `scripts`, `research_reports`를 사용합니다.

실행 진입점:
- `make run`, `make run-single`, `make run-cycle`, `make run-loop`
- `make verify-sources`
